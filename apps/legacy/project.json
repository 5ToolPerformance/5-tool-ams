{
  "name": "legacy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/legacy/src",
  "tags": ["scope:legacy", "type:app", "boundary:compat"],
  "targets": {
    "dev": {
      "command": "next dev .",
      "options": {
        "cwd": "apps/legacy"
      }
    },
    "build": {
      "command": "next build .",
      "options": {
        "cwd": "apps/legacy"
      }
    },
    "start": {
      "command": "next start .",
      "options": {
        "cwd": "apps/legacy"
      }
    },
    "test": {
      "command": "jest --config jest.config.js",
      "options": {
        "cwd": "apps/legacy"
      }
    }
  }
}
